source "https://rubygems.org"

# GitHub Pages builds the site for you, so you don't strictly need this file.
# It's only here so you can preview the blog locally with `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
